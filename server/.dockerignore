# Never bake secrets into image layers — they're provided at runtime via
# env_file and the docker-compose volume mount instead.
.env
service-account.json

node_modules
npm-debug.log
yarn-error.log
.git
.DS_Store
